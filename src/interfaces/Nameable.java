
package interfaces;

// interface which demands for its implementation that the object should have
// a getName() method.
// Its usefulness is that it would be practical to handle in abstract way
// multiple parts of the data with less code.
public interface Nameable {
    
    public String getName();
}
